name: Build cttop

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - "cttop/**"
      - ".github/workflows/cttop-build.yml"
    tags:
      - "cttop-v*"
  pull_request:
    paths:
      - "cttop/**"
      - ".github/workflows/cttop-build.yml"

permissions:
  contents: read

jobs:
  verify:
    name: Format, lint and offline integration
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    defaults:
      run:
        working-directory: cttop
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt, clippy

      - name: Check release version
        if: startsWith(github.ref, 'refs/tags/cttop-v')
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          version=$(python3 -c 'import tomllib; print(tomllib.load(open("Cargo.toml", "rb"))["package"]["version"])')
          test "$RELEASE_TAG" = "cttop-v$version"

      - name: Check formatting and lint
        run: |
          cargo fmt -- --check
          cargo clippy --locked --all-targets -- -D warnings

      - name: Install terminal test dependency
        run: |
          sudo apt-get update -qq
          sudo apt-get install --yes --no-install-recommends python3-pyte

      - name: Test static-file and pipe interactions without privileges
        run: |
          cargo build --locked
          CTTOP_BIN="$PWD/target/debug/cttop" python3 tests/offline.py

  build:
    name: Linux ${{ matrix.arch }} (static musl)
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm
            target: armv7-unknown-linux-musleabihf
          - arch: arm64
            target: aarch64-unknown-linux-musl
          - arch: x86_64
            target: x86_64-unknown-linux-musl
    env:
      CARGO_TERM_COLOR: always
      CARGO_INCREMENTAL: "0"
      RUSTFLAGS: "-C target-feature=+crt-static"
      TARGET: ${{ matrix.target }}
      ARCH: ${{ matrix.arch }}
    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          targets: ${{ matrix.target }}

      - name: Install cross 0.2.5
        working-directory: ${{ runner.temp }}
        run: |
          curl --fail --location --retry 3 \
            https://github.com/cross-rs/cross/releases/download/v0.2.5/cross-x86_64-unknown-linux-musl.tar.gz \
            --output cross.tar.gz
          echo 'a486cefa6cb486971b97be321ea9dfc2e90c1979550295314a368f53fab6d588  cross.tar.gz' | sha256sum --check
          mkdir -p cross-bin
          tar -xzf cross.tar.gz -C cross-bin
          echo "$RUNNER_TEMP/cross-bin" >> "$GITHUB_PATH"

      - name: Run unit tests
        working-directory: cttop
        run: cross test --locked --target "$TARGET"

      - name: Build static executable
        working-directory: cttop
        run: cross build --release --locked --target "$TARGET"

      - name: Smoke test executable
        working-directory: cttop
        run: cross run --release --locked --target "$TARGET" -- -v

      - name: Verify static linkage and prepare executable
        run: |
          binary="cttop/target/$TARGET/release/cttop"
          readelf -lW "$binary" > "$RUNNER_TEMP/elf-program-headers.txt"
          readelf -dW "$binary" > "$RUNNER_TEMP/elf-dynamic-section.txt"
          if grep -E 'INTERP' "$RUNNER_TEMP/elf-program-headers.txt" || \
             grep -E '\(NEEDED\)' "$RUNNER_TEMP/elf-dynamic-section.txt"; then
            echo 'Expected a static executable without an interpreter or shared library dependencies.' >&2
            exit 1
          fi
          file "$binary"
          install -D -m 755 "$binary" "dist/cttop-linux-$ARCH"

      - name: Upload executable
        uses: actions/upload-artifact@v7.0.1
        with:
          path: dist/cttop-linux-${{ matrix.arch }}
          archive: false
          if-no-files-found: error
          retention-days: 30

  release:
    name: Publish GitHub Release
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/cttop-v')
    needs: [verify, build]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}
      GH_REPO: ${{ github.repository }}
      RELEASE_TAG: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@v4

      - name: Check release version
        run: |
          version=$(python3 -c 'import tomllib; print(tomllib.load(open("cttop/Cargo.toml", "rb"))["package"]["version"])')
          test "$RELEASE_TAG" = "cttop-v$version"

      - name: Download executables
        uses: actions/download-artifact@v8
        with:
          pattern: cttop-linux-*
          path: dist
          merge-multiple: true

      - name: Publish executables
        run: |
          for arch in arm arm64 x86_64; do
            test -s "dist/cttop-linux-$arch"
          done
          gh release create "$RELEASE_TAG" \
            dist/cttop-linux-arm dist/cttop-linux-arm64 dist/cttop-linux-x86_64 \
            --draft --verify-tag --title "cttop ${RELEASE_TAG#cttop-}" \
            --notes-file cttop/RELEASE.md
          gh release edit "$RELEASE_TAG" --draft=false
